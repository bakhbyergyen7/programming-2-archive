#main.py
from node import Node

def main():
    front = Node(55)
    temp = Node(17)
    front.next = temp
    temp.next = Node(99)
main()
    #board make the drawing in the notes
    #happen here with code
